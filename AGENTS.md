# agent instructions
reply concisely, avoid wordy formulations
on session start check ./README.md, list the files in ./docs/ ./scripts/ ./tests/ ./scenes/ dirs
before reading classes grep for their exports, consts and func signatures
after large changes run smoke tests, don't run them after minor tweaks
